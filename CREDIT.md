# 크레딧(토큰) 계산 기준 — Seedance 2.0

> **최종 검증**: 2026-08-04 / 모델 `dreamina-seedance-2-0-260128` (fast·mini·4K 일부 포함)
> **성격**: 전부 **실측**이다. 통제 실험 + 계정 전역 1,395건 + **공식 청구 원장과의 대조**로 확인했다. 재현 방법은 §8.
> **짝 문서**: `HANDOFF.md`(앱 전반). 이 문서는 **과금·크레딧 계산**만 다룬다.
>
> ⚠️ 이 저장소는 **Public**이다. 키·토큰·계약 단가·금액을 여기에 적지 마라.
> 실제 단가표와 팀별 정산 금액은 저장소 **밖** `..\26.05.04 시댄스 크레딧 관리\단가표_대외비.md` 에 있다.

---

## 0. 4줄 요약

1. **토큰은 계산할 필요가 없다.** task 조회 응답이 `usage.total_tokens`로 실제 사용량을 직접 준다.
2. **과금되는 건 오직 "프레임"이다.** 프롬프트 텍스트 0토큰, 레퍼런스 이미지 0토큰. 출력 프레임 + 입력 영상 프레임만 센다.
3. **task ID 하나로 단가까지 확정된다.** 응답의 duration·fps·resolution으로 입력 영상 초를 **정수로 정확히 역산**할 수 있다(§4). 1,376건 전부 정수로 떨어졌다.
4. **★ 목록 API는 "키별"이 아니라 "계정 전역"이다.** 14개 팀 키 전부가 **완전히 동일한 집합**을 돌려준다(§3-2). 조회는 키 하나면 충분하고, **팀별 귀속은 목록 API로는 불가능하다**(§11).

---

## 1. 확정 공식

```
토큰 = 총프레임수 × W × H / 1024        (소수점 버림)

총프레임수 = (출력 duration × fps + 1)          ← 출력 몫
           + (입력영상 초 × fps)                 ← 입력영상 몫 (없으면 0)
```

- `+1`은 실측값이다. 출력 5초/24fps 영상의 실제 프레임이 **121**이었고(ffprobe), 토큰도 121프레임 기준과 정확히 일치했다.
  BytePlus 공식 문서의 예시 계산은 `duration × fps`(=120)로 되어 있어 **0.8% 차이**가 난다.

### 해상도·비율별 픽셀 상수

| 해상도 | 비율 | W×H | 초당 토큰(24fps) | 근거 |
|---|---|---:|---:|---|
| 480p | 16:9 / 9:16 | **428,544** (864×496) | 10,044 | ffprobe |
| 480p | 1:1 | **409,600** (640×640) | 9,600 | 실측(adaptive 응답) |
| 720p | 16:9 | **921,600** (1280×720) | 21,600 | ffprobe |
| 720p | 4:3 | ≈ 927,405 | ≈21,736 | 역산 하한 |
| 1080p | 16:9 | **2,073,600** (1920×1080) | 48,600 | ffprobe |
| 1080p | 21:9 | ≈ 2,086,870 | ≈48,911 | 역산 하한 |
| 4K | 16:9 | **8,294,400** (3840×2160) | 194,400 | 정합 42건 |
| 4K | 21:9 | ≈ 8,294,620 | ≈194,405 | 역산 하한 |

> **"역산 하한"의 뜻**: 토큰이 내림되므로 `tokens×1024/프레임수`는 실제보다 최대 몇 px 작게 나온다.
> 정확한 값이 필요하면 그 비율로 1건 뽑아 **ffprobe로 재라**. 480p 16:9도 역산으로는 428,539였고 실제는 428,544였다.
> 가로/세로는 픽셀 수가 같아 토큰도 같다(480p 16:9 = 480p 9:16).

### 자주 쓰는 조합 (영상 입력 없을 때)

| 출력 | 480p | 720p | 1080p | 4K |
|---|---:|---:|---:|---:|
| 5초 | 50,638 | 108,900 | 245,025 | 980,100 |
| 10초 | 100,858 | 216,900 | 488,025 | 1,952,100 |
| 15초 | 151,078 | 324,900 | 731,025 | 2,924,100 |

---

## 2. 무엇이 과금되고, 무엇이 안 되나

| 항목 | 과금 | 실측 근거 |
|---|:---:|---|
| 출력 영상 프레임 | ✅ | 공식이 정확히 일치 |
| **입력(레퍼런스) 영상** | ✅ | 4초 영상 첨부 시 정확히 2배(40,594 → 80,770) |
| 프롬프트 텍스트 | ❌ **0** | 75자와 **14,680자**가 토큰 완전 동일 |
| 레퍼런스 **이미지** | ❌ **0** | 이미지 0장과 1장이 토큰 완전 동일 |
| **실패·취소된 task** | ❌ | `usage` 필드 자체가 없다 → 필터가 공짜 |

**요점**: 레퍼런스 이미지는 몇 장을 붙이든 비용이 늘지 않는다. 비용을 늘리는 건 **출력 길이·해상도**와 **입력 영상 길이**뿐이다.

---

## 3. 조회 API

### 3-1. 단건 조회 — `GET /contents/generations/tasks/{id}`

| 필드 | 용도 |
|---|---|
| `usage.total_tokens` | **실제 청구 토큰** (= `completion_tokens`와 항상 동일) |
| `duration` | 출력 길이(초) |
| `framespersecond` | fps (우리가 보낸 적 없는데 채워서 옴) |
| `resolution` | `480p` / `720p` / `1080p` / `4k` |
| `ratio` | `16:9` 등 |
| `model` | 모델 id |

**adaptive / Auto 도 "값"으로 온다** — `ratio:"adaptive"` → `"9:16"`(또는 `"1:1"`), `duration:-1` → `5`.
어떤 조합으로 보냈든 응답은 확정된 실제 값을 주므로 **판별 불가 예외는 없다.**

**주의**: `total_tokens == completion_tokens`라 `total − completion`으로 입력분을 뽑으려는 시도는 무의미하다(항상 0).

### 3-2. ★ 목록 조회 — `GET /contents/generations/tasks?page_size=200&page_num=N`

**task ID를 몰라도 그 계정이 만든 작업 전부가 나온다.** 각 항목에 `usage`가 이미 붙어 있다.

- 200건/페이지, `total` 필드로 전체 수 확인. 1,395건을 14페이지로 **누락·중복 0**으로 수집 검증.
- **★ 키 단위가 아니라 계정 단위다.** 팀 키 14개로 각각 긁어 task ID 집합을 비교했더니 **14개 전부 완전히 동일**했다. 조회는 아무 키 하나면 된다.
- **작업 객체에 프로젝트/키 식별자가 없다.** 그래서 이걸로는 팀별 귀속이 안 된다 → §11.
- 보존 **정확히 7.00일**(§8).

---

## 4. 비디오 입력 판별 = 요율 선택

요율이 **입력 영상 유무로 갈리므로**(§6) 이 판별이 곧 단가 선택이다.

```
1) 응답에서 duration, framespersecond, resolution, ratio 읽기
2) 픽셀 = §1 표에서 조회
3) 입력초 = (total_tokens × 1024 / 픽셀 − 1) / fps − duration
4) 입력초가 0에 가까우면 영상 없음, 양의 정수면 그 길이만큼 영상 입력 있음
```

**임계값이 필요 없다.** 계정 전역 1,376건에 적용한 결과 **전부 정수**로 떨어졌다(0·3·4·5·6·9·10·11·15초, 비정수 0건).
유무만이 아니라 **입력 영상이 몇 초였는지까지** 나온다.

> 이전 버전은 `total > 기준선 × 1.05` 임계값을 썼다. 그것도 안전하지만(레퍼런스 영상은 API가 2~15초로
> 제한해 최소 신호가 +13.3%), 정수 역산이 더 강하고 정보량도 많으므로 이쪽을 쓴다.

---

## 5. ★ 절대 토큰량으로 판별하면 틀린다

**같은 토큰량이 서로 다른 상황에서 나올 수 있다.** 480p 실제 계산 예:

| | 실제 토큰 | 그 task의 기준선 | 비율 | 판정 |
|---|---:|---:|---:|---|
| X. 영상 없이 **15초** 출력 | **151,078** | 151,078 | 1.00 | 영상 X |
| Y. 영상 11초 + **4초** 출력 | **151,078** | 40,594 | 3.72 | 영상 O |

토큰이 **151,078로 완전히 동일**하다. "N토큰 넘으면 영상"식 임계값은 여기서 100% 틀린다.

**그런데도 판별이 되는 이유**: 절대량끼리 비교하지 않고 **각 task를 자기 자신의 기준선과** 비교하기 때문이다. 응답이 출력 duration(15 vs 4)을 알려주므로 기준선이 갈라진다.

---

## 6. 요율 구조

청구서(`ListSplitBillDetail`)에 실제로 찍히는 **과금 항목 이름**은 다음 10종이다. 단가는 대외비 — 위 헤더의 로컬 문서 참조.

```
Dreamina-Seedance-2.0-inference-non-video-in            ← 480p·720p, 영상입력 없음
Dreamina-Seedance-2.0-inference-non-video-in-1080p
Dreamina-Seedance-2.0-inference-non-video-in-4k
Dreamina-Seedance-2.0-inference-video-in                ← 영상입력 있음
Dreamina-Seedance-2.0-inference-video-in-1080p
Dreamina-Seedance-2.0-inference-video-in-4k
Dreamina-Seedance-2.0-fast-inference-{non-,}video-in
Dreamina-Seedance-2.0-mini-inference-{non-,}video-in
```

요율을 가르는 축은 **세 개**다:

1. **모델 티어** — 기본 / `fast` / `mini`
2. **입력 영상 유무** — ★ 있으면 **그 task 전체 토큰**이 더 싼 요율로 간다(입력분만이 아니다)
3. **해상도 구간** — `480p·720p`(구분 없음) / `1080p` / `4k`

**단가 크기 관계** (실측):

- 같은 해상도에서 **영상입력 있음 < 없음** (토큰당 약 40% 저렴)
- 해상도는 **4K < 480p·720p < 1080p** — ★ **4K가 토큰당 가장 싸다.** 대신 토큰이 4배라 총액은 훨씬 크다.
- `mini` < `fast` < 기본

**주의할 착시**: 영상을 넣으면 토큰이 크게 늘지만 단가가 낮아진다. 1080p 5초 기준 실제로는
`245,025토큰 × 비싼요율` → `488,025토큰 × 싼요율`로 **총액이 약 +22%**다. "토큰이 2배니까 비용도 2배"가 아니다.

### 검산이 된다

계정 전역 목록 API만으로 위 분류를 재현해 **공식 청구서와 대조했다.** 2026-07-31(정산 완료된 하루)에서
**9개 과금 항목 전부가 토큰 단위까지 정확히 일치**했다(fast·mini 티어, 영상입력 유무, 해상도 구간 포함).
8/1·8/2도 완전 일치. → **목록 API + §1·§4만으로 클립별 정확 원가를 낼 수 있다.**

---

## 7. 실측 데이터

### 7-1. 통제 실험 (480p·4~5초·24fps·16:9, 나머지 조건 동일)

| # | 입력 구성 | 토큰 | 기준선 대비 | 확인한 것 |
|---|---|---:|---:|---|
| A | 텍스트만 (75자) | 50,638 | 1.00 | 기준선 |
| B | + 이미지 1장 | 50,638 | 1.00 | **이미지 0토큰** |
| C | + 비디오 1개 (4초) | **80,770** | **2.00** | **영상은 프레임으로 가산** |
| D | 텍스트 14,680자 | 50,638 | 1.00 | **텍스트 0토큰** |
| E | 720p | 108,900 | 1.00 | 720p = 1280×720 |
| F | ratio=adaptive + duration=Auto | 40,594 / 38,800 | 1.00 | **응답은 실제 값(16:9·1:1 / 5초)** |

검산: `121 × 428,544 / 1024 = 50,638` · `121 × 921,600 / 1024 = 108,900` — 오차 0.

### 7-2. 계정 전역 대조 (1,395건, 7일치)

| 항목 | 결과 |
|---|---|
| 공식 사용량 지표(`GetUsage`)와 일별 토큰 | **7/30·7/31·8/1·8/2 완전 일치(차이 0)** |
| 경계일 차이 | 7/28↔7/29 ±731,025 — 자정에 걸친 15초 1080p **1건**이 생성시각/정산시각으로 갈린 것 |
| 공식 청구서 항목 재현 | 7/31 **9/9 항목 일치** |
| 모델별 공식 정합 | 기본 598/598, fast 1/1, mini 2/2 |

---

## 8. 재현 / 검증 방법

### 8-1. 통제 실험 (앱 서버 경유)

```bash
# 1) 파일을 캐시에 넣고 cacheId 받기
curl -s -X POST "http://localhost:3000/api/cache" \
  -H "x-filename: ref.mp4" --data-binary @ref.mp4

# 2) cacheId → R2 presigned URL (★ task마다 새로 발급할 것)
curl -s -X POST "http://localhost:3000/api/reupload/<cacheId>"

# 3) task 생성 (payload는 content 배열에 text/image_url/video_url)
curl -s -X POST "http://localhost:3000/api/byteplus/tasks" \
  -H "Content-Type: application/json" --data @payload.json

# 4) 조회 — usage.total_tokens 확인
curl -s "http://localhost:3000/api/byteplus/tasks/<taskId>"
```

> 원가 검증용 생성은 **앱 서버를 거치지 말고 BytePlus에 직접** 쏘는 게 낫다. 앱 서버를 경유하면
> 트래커 시트에 테스트 행이 쌓인다. (직접 쏘면 트래커에 안 남는 게 정상이다 — 나중에 "누락"으로 오해하지 말 것.)

### 8-2. 어드민 OpenAPI (계정 전체 감사)

AK/SK로 서명해 호출한다. 서명 구현과 실행 스크립트는 저장소 **밖**:
`..\26.05.04 시댄스 크레딧 관리\tracker_capture.mjs` · `tracker_healthcheck.mjs`

| Action | Service | 주는 것 |
|---|---|---|
| `GetUsage` | `ark` | 일별 `CompletionTokens`·`ImageCount`. **`ProjectName` 필터 가능** |
| `ListApiKeys` | `ark` | 프로젝트별 API 키 대장(마스킹된 앞4·뒤4) |
| `ListProjects` | `iam` | 프로젝트 전체 |
| `ListSplitBillDetail` | `billing` | **프로젝트별 확정 요금** |
| `ListBillDetail` | `billing` | 일별 × 과금항목 × 토큰량 |

**호출 함정 5개** (전부 밟아봤다):

1. **`GetUsage`는 창마다 포인트가 겹쳐 나온다.** 그냥 더하면 최대 2배로 부풀려진다 — 반드시 **타임스탬프를 키로 덮어쓰기**.
2. **창이 길면 오히려 포인트를 흘린다** (31일 요청 → 29일치만). **3일씩** 끊어라.
3. **일별 버킷 경계는 UTC가 아니라 UTC+8**이다.
4. **Secret Access Key는 콘솔 문자열 그대로** 쓴다(base64로 보이지만 디코딩하면 서명 실패).
5. ark 계열 액션은 **POST 바디**, billing 계열은 **쿼리스트링**이다. 섞으면 `MissingParameter`.

### 8-3. 보존 기한 — **정확히 7.00일**

- **task 단건/목록 조회**: 7일. 8일 지난 ID는 404. **하루라도 안 긁으면 그날 클립별 내역은 영구 소실.**
- **영상 URL**: 24시간 (`X-Tos-Expires=86400`).
- **`GetUsage`**: 최소 40일 이상 조회됨(총량만, 클립 단위 아님).
- **청구서**: 월 단위로 계속 남음.

→ **클립 단위 원가를 남기려면 매일 1회 목록 API 전체를 긁어 저장해야 한다.**

---

## 9. 미검증 / 유보 사항

정직하게 남긴다.

1. **일부 비율의 정확한 W×H** — 720p 4:3 · 1080p 21:9 · 4K 21:9 는 역산 **하한**만 있다(§1). 몇 px 오차가 있을 수 있다. 정확히 필요하면 ffprobe.
2. **`+1` 프레임의 출처** — 실측은 121프레임인데 공식 문서 예시는 120이다. 0.8% 차이의 이유(문서 단순화 vs 실제 동작)는 미확인. 계산에는 실측값을 쓴다.
3. **fast / mini 표본** — 공식에 정합하지만 각 1건·2건뿐이다. 상수가 같다고 단정하기엔 표본이 작다.
4. **2.5 Demo** — 별도 계약·별도 계정이라 이 문서 범위 밖이다. 그 계정의 task 목록은 **공유 계정**이라 우리 것이 아닌 작업이 섞여 있다(HANDOFF 참조).
5. **`GetUsage` vs 청구서의 미세 차이** — 최근 며칠은 `GetUsage`가 청구서보다 크다(정산 진행 중). 오래된 날짜는 0으로 수렴한다. 끝내 안 붙는 분(리소스 패키지 커버 등)이 있는지는 미확인.

---

## 10. 트래커(구글시트) 연동 현황

### 지금 보내는 것 (`server.ts`, task 조회가 succeeded일 때 1회)

```
team, project, task_id, total_tokens, completion_tokens,
resolution, model, source, timestamp
```

- **팀 판별**: `SEEDANCE_API_KEY`를 SHA-256 해싱해 내장 맵과 대조 → `TEAM_NAME`. 키 자체는 번들에 없다.
- **dedupe**: `reportedTasks`로 task당 1회.
- **조건**: `succeeded` + `usage.total_tokens` 존재 + 데모 아님.
- Apps Script가 `resolution`·`model`은 현재 **버린다**(로그 행 수 관리). 되살리는 건 Apps Script 한 줄 — 앱 재배포 불필요.

### 실측 포착률 — **97.83%** (2026-08-04 기준)

`usage_log` 25,476행 전부를 공식 사용량과 대조했다.

| | |
|---|---|
| 전체 포착률 | **97.83%** (토큰) / 98.3% (최근 7일 건수) |
| 팀별 범위 | 93.7% ~ 100.0% |
| 미포착 | 약 2.2% |

**측정할 때 반드시 지킬 것 — 시작일**:

- 트래커 POST는 **앱 v26.5.401(2026-05-04, 커밋 `53ef6c9`)** 에서 처음 들어갔다. 그 전 생성은 usage_log에 행이 **없는 게 정상**이다.
- 게다가 **팀마다 앱을 올린 시점이 다르다.** 팀별 첫 기록일:
  `1팀·AFX·Special 05-04` / `4팀 05-05` / `6팀·3팀 05-06` / `5팀·8팀 05-07` /
  `2팀 05-12` / `10팀 05-13` / `7팀 05-14` / **`9팀 05-26`** / **`TA팀·AIP 06-02`**
- 이걸 무시하고 전역 시작일로 재면 **9팀이 27%로 보인다**(실제 98.5%). 반드시 팀별 첫 기록일로 잴 것.

**미포착 2.2%의 성격**: 최근 7일 누락 23건을 전수 확인한 결과 **해상도·길이와 상관관계가 없다**(1080p 4~15초, 4K, 720p가 섞여 있음). 트래커 POST는 폴링이 `succeeded` 응답을 봐야 나가므로, **결과를 받기 전에 앱이 닫힌 경우**로 보인다. 줄이려면 앱 재시작 시 미보고 task를 재확인하는 로직이 필요하다.

### `project` 열은 7월부터만 있다

`taskToProject`(앱의 프로젝트 드롭다운 → 트래커)는 **v26.7.0201(2026-07-02)** 에 들어왔다.
25,476행 중 **7,371행만** `project`가 채워져 있다. **7월 이전 클립은 프로젝트 귀속이 불가능하다.**

### 더 확실한 대안

판별(추론)을 아예 안 하는 방법: **task 생성 시점에 앱이 이미 알고 있다.** `content` 배열에 `video_url`이 있는지가 곧 답이다. `has_video_input: true/false` 한 필드를 추가하면 사후 추론이 필요 없다.

> §4는 **이미 쌓인 로그를 소급 계산**할 때, `has_video_input`은 **앞으로 쌓일 로그**를 확실하게 만든다. 배타적이지 않다.

---

## 11. 팀별 정산은 어떻게 하나

세 소스를 **`task_id`로 조인**해야 완전해진다.

| 소스 | 주는 것 | 보존 |
|---|---|---|
| 목록 API (키 1개) | 클립별 명세 → **정확한 원가** | **7일** |
| 구글시트 `usage_log` | `task_id → 팀 · 프로젝트` | 전 기간 |
| 어드민 `GetUsage` + `ProjectName` | 팀별 일별 총 토큰(검산용) | 40일+ |
| 어드민 `ListSplitBillDetail` | **팀별 확정 요금** | 월 단위 영구 |

**주의점**

- 목록 API에는 팀/프로젝트 필드가 **없다**(§3-2). 시트 없이는 팀별 귀속 불가.
- `GetUsage`에 먹히는 필터는 **`ProjectName` 하나뿐**이다. 모델·해상도·영상입력으로는 못 쪼갠다.
  → 팀별 **총 토큰**은 정확하지만, 팀별 **항목별** 내역은 시트 조인이 있어야 한다.
- **BytePlus 프로젝트명 ≠ 팀명.** 특히 **`TA팀`의 키는 `2DT` 프로젝트에 달려 있다.** 매핑은
  `dashboard_byteplus.gs`의 `BP_PROJECT_TO_TEAM` 과 위 감사 스크립트에 동일하게 박혀 있다.
- **팀 bat이 없는 계정 키가 5개 있다** (`default`, `DS5`, `RND`, `RND_Mimesis_1`, `RND_Mimesis_2`).
  이들 사용량은 트래커에 전혀 안 잡힌다. 팀 정산에 포함하려면 bat과 `TEAM_KEY_HASHES` 등록이 필요하다.
- 대시보드는 `bp_archive`(BytePlus 공식)가 있으면 **오늘 이전 날짜를 전부 그걸로 덮어쓴다.**
  그래서 대시보드 숫자를 공식 API와 비교하면 **공식 vs 공식**이라 항상 맞는다. 트래커 포착률을 재려면
  `lifetimeCount × teamAvg`처럼 **덮이지 않는 필드**를 써야 한다(§10, 감사 스크립트 참조).
- `syncByteplusArchive()`는 **당월만** 재동기화하고 전월은 매월 1~3일에만 돈다.
  → **4일부터 지난달 아카이브는 동결**된다. 뒤늦게 귀속된 요금을 반영하려면 `backfillByteplusArchive()` 수동 실행.

---

_작성 2026-08-03, 갱신 2026-08-04. 수치를 바꾸는 실측을 했으면 이 문서를 갱신할 것._
